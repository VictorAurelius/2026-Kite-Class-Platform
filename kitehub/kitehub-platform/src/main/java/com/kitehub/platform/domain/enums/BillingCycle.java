package com.kitehub.platform.domain.enums;

/**
 * Billing cycle options for subscriptions.
 *
 * @author KiteHub Team
 * @since 1.0.0
 */
public enum BillingCycle {
    /**
     * Monthly billing (charged every month).
     */
    MONTHLY,

    /**
     * Annual billing (charged once per year, 10% discount).
     */
    ANNUALLY
}
