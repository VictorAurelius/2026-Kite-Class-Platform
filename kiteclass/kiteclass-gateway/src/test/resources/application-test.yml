spring:
  test:
    mockmvc:
      print: false
    context:
      bean:
        override:
          enabled: true  # Enable bean override for @MockitoBean
  main:
    banner-mode: off

logging:
  level:
    root: WARN
    com.kiteclass: INFO
