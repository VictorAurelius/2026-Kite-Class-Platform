<?xml version="1.0" encoding="UTF-8"?>
<!--
    GAP-114 / GAP-116 — Structured JSON logging baseline (kiteclass-core).

    Applies the schema from .claude/rules/logs-format-standard.md §2.
    PIIScrubberConverter lives in kitehub-platform (not on kiteclass classpath);
    kiteclass services rely on Jackson @Redact + structured-arg discipline; a
    locally-shipped PIIScrubber tracked in GAP-116-followup-existing-code-pii-audit.
-->
<configuration scan="true" scanPeriod="60 seconds">

    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="kiteclass-core"/>
    <springProperty scope="context" name="appProfile" source="spring.profiles.active" defaultValue="default"/>

    <appender name="JSON_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"${serviceName}"}</customFields>
            <includeMdcKeyName>tenantId</includeMdcKeyName>
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>requestId</includeMdcKeyName>
        </encoder>
    </appender>

    <appender name="PLAIN_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} [tenant=%X{tenantId:--} trace=%X{traceId:--}] - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="local | dev">
        <root level="INFO">
            <appender-ref ref="PLAIN_STDOUT"/>
        </root>
    </springProfile>

    <springProfile name="!local &amp; !dev">
        <root level="INFO">
            <appender-ref ref="JSON_STDOUT"/>
        </root>
    </springProfile>

    <logger name="com.kiteclass" level="INFO"/>
</configuration>
