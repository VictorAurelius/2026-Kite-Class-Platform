<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.kiteclass</groupId>
    <artifactId>kiteclass-core</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>KiteClass Core Service</name>
    <description>Core business service for KiteClass Platform</description>

    <properties>
        <java.version>17</java.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <springdoc.version>2.8.17</springdoc.version>
        <jjwt.version>0.13.0</jjwt.version>
        <aws-sdk.version>2.44.4</aws-sdk.version>
        <tika.version>3.3.0</tika.version>
        <resilience4j.version>2.4.0</resilience4j.version>
        <!-- Override Jackson version to fix GHSA-72hv-8253-57qq (DoS vulnerability) -->
        <jackson.version>2.21.1</jackson.version>
        <!-- GAP-203: pinned to fix transitive CVEs surfaced via GitHub code-scanning 2026-04-21 -->
        <!-- Spring Boot 3.5.13 ships Tomcat 10.1.53; override resolves CVE-2026-34483/34487/34500 -->
        <tomcat.version>10.1.54</tomcat.version>
        <commons-beanutils.version>1.11.0</commons-beanutils.version>
        <commons-compress.version>1.28.0</commons-compress.version>
        <poi.version>5.5.1</poi.version>
        <!-- Wave 5 ADR-019: document generation stack. openhtmltopdf-pdfbox 1.0.x pulls Apache PDFBox 2.0.x transitively. -->
        <openhtmltopdf.version>1.0.10</openhtmltopdf.version>
        <!-- GAP-114: structured JSON logging via LogstashEncoder (Spring Boot 3.5.x compatible) -->
        <logstash-logback-encoder.version>8.0</logstash-logback-encoder.version>
        <!-- GAP-468 + GAP-470: CVE overrides for transitive deps surfaced via GitHub code-scanning 2026-05-11 -->
        <!-- netty 4.2.13.Final: GAP-470 line bump for revised CVE-2026-42577 (advisory has no 4.1.x patch) -->
        <netty.version>4.2.13.Final</netty.version>
        <postgresql.version>42.7.11</postgresql.version>
        <bouncycastle.version>1.84</bouncycastle.version>
        <!-- commons-lang3 3.18.0 fixes CVE-2025-48924 (Spring Boot 3.5.14 ships 3.17.0) -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- GAP-203: CVE overrides for transitive deps -->
            <dependency>
                <groupId>commons-beanutils</groupId>
                <artifactId>commons-beanutils</artifactId>
                <version>${commons-beanutils.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>

            <!-- GAP-468: netty-bom override forces all netty-* artifacts to 4.1.133.Final (closes 5 HIGH CVE) -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- GAP-569: CVE-2026-45292 OpenTelemetry Unbounded Memory Allocation in W3C Baggage Propagation (MEDIUM).
                 Spring Boot 3.5.14 BOM pins OTel 1.49.0 (affected); bump via opentelemetry-bom 1.62.0.
                 Affected (transitive via opentelemetry-exporter-otlp + micrometer-tracing-bridge-otel):
                   - io.opentelemetry:opentelemetry-api
                   - io.opentelemetry:opentelemetry-extension-trace-propagators -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>1.62.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- GAP-468: bcprov-jdk18on 1.84 (CVE-2026-5598) -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- Spring Boot 3.5.14 BOM hard-pins these (property override alone không thắng); cần explicit dependencyManagement entry -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Utilities -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>

        <!-- OpenAPI Documentation -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Monitoring -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- GAP-112: Distributed tracing — Micrometer Tracing Bridge to OpenTelemetry + OTLP exporter.
             Versions managed by Spring Boot 3.5 BOM. Auto-instruments Spring MVC, RestTemplate,
             RestClient, JDBC, and Spring AMQP (RabbitMQ traceparent header propagation). -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>

        <!-- GAP-114: structured JSON logging encoder; consumed by logback-spring.xml -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>${logstash-logback-encoder.version}</version>
        </dependency>

        <!-- Resilience (Circuit Breaker + Bulkhead + Retry) per ADR-008 -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-micrometer</artifactId>
            <version>${resilience4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- AWS SDK for S3 (MinIO compatible) -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>${aws-sdk.version}</version>
        </dependency>

        <!-- Apache Tika for file type detection -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>${tika.version}</version>
        </dependency>

        <!-- JSoup for SVG sanitization (Wave 4 Sub-PR 4.2, ADR-011) -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.22.2</version>
        </dependency>

        <!-- Apache POI for xlsx parsing + generation (GAP-051 Wave 1, bulk import students) -->
        <!-- GAP-203: bumped 5.2.5 → 5.4.0 to fix CVE-2025-31672 (zip entry duplicate name) -->
        <!-- Wave 5 GAP-047: POI also hosts XWPF (Word) for DocxGenerator in Sub-PR 5.3 -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>

        <!-- Wave 5 (ADR-019 / GAP-047): Document generation stack -->
        <!-- OpenHTMLtoPDF: HTML → PDF rendering. Pulls Apache PDFBox 2.0.x transitively (LGPL 3 / MIT). -->
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-core</artifactId>
            <version>${openhtmltopdf.version}</version>
        </dependency>
        <dependency>
            <groupId>com.openhtmltopdf</groupId>
            <artifactId>openhtmltopdf-pdfbox</artifactId>
            <version>${openhtmltopdf.version}</version>
        </dependency>
        <!-- Thymeleaf for templating HTML inputs to PDF (version from Spring Boot BOM) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <!-- OGNL: required by Thymeleaf's Standard dialect when used WITHOUT Spring integration
             (direct TemplateEngine in InvoiceRenderer). Spring-native rendering uses SpEL via
             thymeleaf-spring6 and does not need this.

             PINNED at 3.3.4 — the version Thymeleaf 3.1.x parent pom declares. The 3.4.x line
             dropped the OgnlContext(MemberAccess, ClassResolver, TypeConverter, Map) constructor
             that Thymeleaf 3.1.5 calls reflectively from OGNLVariableExpressionEvaluator (Map →
             OgnlContext signature change). Result: NoSuchMethodError on every Thymeleaf evaluation.
             Earlier Wave 5 PRs declared 3.4.4 by mistake; tests passed only because Thymeleaf was
             a different version then. Hold at 3.3.4 until Thymeleaf upstream catches up;
             do NOT let Dependabot bump this without running PdfGeneratorTest first. -->
        <dependency>
            <groupId>ognl</groupId>
            <artifactId>ognl</artifactId>
            <version>3.3.5</version>
        </dependency>

        <!-- OpenCSV — CSV parsing (GAP-051, reserved for CSV bulk import variant) -->
        <dependency>
            <groupId>com.opencsv</groupId>
            <artifactId>opencsv</artifactId>
            <version>5.12.0</version>
        </dependency>

        <!-- Testcontainers MinIO -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>minio</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Suppress m2e warning for checkstyle -->
                <plugin>
                    <groupId>org.eclipse.m2e</groupId>
                    <artifactId>lifecycle-mapping</artifactId>
                    <version>1.0.0</version>
                    <configuration>
                        <lifecycleMappingMetadata>
                            <pluginExecutions>
                                <pluginExecution>
                                    <pluginExecutionFilter>
                                        <groupId>org.apache.maven.plugins</groupId>
                                        <artifactId>maven-checkstyle-plugin</artifactId>
                                        <versionRange>[3.0,)</versionRange>
                                        <goals>
                                            <goal>check</goal>
                                        </goals>
                                    </pluginExecutionFilter>
                                    <action>
                                        <ignore/>
                                    </action>
                                </pluginExecution>
                            </pluginExecutions>
                        </lifecycleMappingMetadata>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>0.2.0</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-Amapstruct.defaultComponentModel=spring</arg>
                        <!-- Treat deprecation warnings as visible in build output -->
                        <arg>-Xlint:deprecation</arg>
                        <!-- Show unchecked warnings -->
                        <arg>-Xlint:unchecked</arg>
                    </compilerArgs>
                    <!-- Ensure warnings are shown in output -->
                    <showWarnings>true</showWarnings>
                    <showDeprecation>true</showDeprecation>
                </configuration>
            </plugin>
            <!--
              JaCoCo: surefire (unit) + failsafe (integration) coverage merge.

              Sonar reads ONE merged exec → ONE merged jacoco.xml. Without merge,
              classes covered ONLY by *IT (full-stack @SpringBootTest / @WebMvcTest)
              tests appear uncovered to Sonar → false 78.2% on PR #773 new code
              (gate ≥80%). Spring Boot's parent already splits *Test (surefire)
              vs *IT (failsafe); this config wires the JaCoCo agent to BOTH and
              merges the two .exec files before report.

              Phase wiring:
                - prepare-agent           → before surefire (writes target/jacoco.exec)
                - prepare-agent-integration → before failsafe (writes target/jacoco-it.exec)
                - merge                   → after integration-test (target/jacoco-merged.exec)
                - report                  → during verify (reads merged exec, writes target/site/jacoco/jacoco.xml)

              Required CI invocation: `mvn verify` (NOT `mvn test`). `mvn test` skips
              integration-test + verify phases → no IT coverage merged.
            -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <!-- Surefire agent: writes target/jacoco.exec (default) -->
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>

                    <!-- Failsafe agent: writes target/jacoco-it.exec, bound to pre-integration-test -->
                    <execution>
                        <id>prepare-agent-integration</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco-it.exec</destFile>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>

                    <!-- Merge surefire + failsafe execs → target/jacoco-merged.exec -->
                    <execution>
                        <id>merge-results</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>merge</goal>
                        </goals>
                        <configuration>
                            <fileSets>
                                <fileSet>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>jacoco.exec</include>
                                        <include>jacoco-it.exec</include>
                                    </includes>
                                </fileSet>
                            </fileSets>
                            <destFile>${project.build.directory}/jacoco-merged.exec</destFile>
                        </configuration>
                    </execution>

                    <!-- Generate report from merged exec → target/site/jacoco/jacoco.xml (Sonar default lookup path) -->
                    <execution>
                        <id>report-merged</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <dataFile>${project.build.directory}/jacoco-merged.exec</dataFile>
                            <outputDirectory>${project.build.directory}/site/jacoco</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
              Failsafe: explicitly declare so the prepare-agent-integration argLine
              is wired in. Spring Boot's parent inherits a failsafe binding to
              integration-test + verify phases, but does NOT wire the JaCoCo
              argLine — without this, the agent is silently dropped and *IT
              coverage is lost.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <argLine>${failsafeArgLine}</argLine>
                </configuration>
                <executions>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Checkstyle Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <configLocation>${project.basedir}/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <violationSeverity>error</violationSeverity>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--
      GAP-245 Phase 1 — strict-warnings profile.

      Activated in CI (`mvn -P strict-warnings ...`). Default build already shows
      -Xlint:deprecation + -Xlint:unchecked; this profile adds raw-types, cast,
      and overrides checks on top.

      Phase 1 (this PR): warnings are SHOWN but build still passes (no -Werror).
      Phase 2 (GAP-261-werror-flipday): inventory + triage existing warnings,
      then flip CI profile to add `-Werror`.

      Note: kiteclass has no parent POM — each module declares its own profile.
    -->
    <profiles>
        <profile>
            <id>strict-warnings</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <showWarnings>true</showWarnings>
                            <showDeprecation>true</showDeprecation>
                            <compilerArgs>
                                <arg>-Amapstruct.defaultComponentModel=spring</arg>
                                <arg>-Xlint:deprecation</arg>
                                <arg>-Xlint:unchecked</arg>
                                <arg>-Xlint:rawtypes</arg>
                                <arg>-Xlint:cast</arg>
                                <arg>-Xlint:overrides</arg>
                            </compilerArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
