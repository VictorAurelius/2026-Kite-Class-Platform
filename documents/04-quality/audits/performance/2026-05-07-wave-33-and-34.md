# Performance Audit — Wave 33 + Wave 34

**Date:** 2026-05-07
**Auditor:** Background agent abeb4c4b (Sonnet, Explore subagent)
**Scope:** Wave 33 BetaAccessController + Wave 34 BrandingWizardController/DeployStreamController/InstanceLifecycleService + V28/V29/V30 migrations
**Mode:** Static-analysis (dev stack chưa boot per GAP-244 — không runtime metrics, không profiling, không load test)
**Baseline:** 2026-04-25 = 63/100 D (Sub-PR 5.6a #530)

---

## Score: 58/100 — F (delta -5 vs baseline)

| # | Category | /20 | Notes |
|:-:|----------|:---:|-------|
| 1 | DB query patterns | 10/20 | **CRITICAL N+1** `SlugAvailabilityService.isTaken()` — `findAll()` rồi stream/normalize/match in-memory. Zero index `BrandingJob.organization_name`. O(n) scale với job count |
| 2 | API handler latency | 12/20 | `@Timed` annotations OK; regenerate endpoint sync idempotency replay (2 DB query back-to-back); SSE poller 2s interval không backpressure |
| 3 | Bundle size + FE perf | 14/20 | Wizard 149kB baseline acknowledged; chưa thấy dynamic imports; slug check round-trip mỗi keystroke (debounce 600ms acknowledged) |
| 4 | Cache strategy | 12/20 | Caffeine 2-cache (templates + rate-limit); **`RegenerateQuotaService.getQuota()` hits DB mỗi call** — không cache; `SlugAvailabilityService` 0 cache |
| 5 | Async work + RabbitMQ | 10/20 | DLQ + retry 3× exponential backoff OK; SSE poller `@Scheduled` không concurrency control; queue-depth monitoring absent |

---

## Top 5 Findings

| # | Sev | Issue | File:Line | Fix |
|---|:---:|-------|-----------|-----|
| 1 | 🔴 **P0 BLOCKING** | N+1 slug lookup — `findAll()` scans entire BrandingJob table mỗi `/api/v1/branding/slug-availability` call. FE keystroke → full table scan | `SlugAvailabilityService.java:85` | `@Query("SELECT DISTINCT LOWER(o.organizationName) FROM BrandingJob o WHERE LOWER(o.organizationName) = :slug")` + `@Index(name="idx_job_org_name_lower", columnList="organization_name")` |
| 2 | 🟠 P1 | `RegenerateQuotaService.getQuota()` hits DB mỗi call (no cache) | `RegenerateQuotaService.java:72` | `@Cacheable(value="regenerateQuota", key="#userId+#windowStart")` 1min TTL |
| 3 | 🟠 P1 | SSE deploy-stream poller no backpressure — slow FE → emitter sends pile up | `DeployStreamController.java:119, 133` | Add `emitter.send()` write-timeout + IOException catch + cleanup |
| 4 | 🟠 P1 | Missing index `BrandingJob.status` — `findByStatus()` poll loop scan | `BrandingJobRepository.java:43` + entity | V31 migration `CREATE INDEX idx_branding_job_status ON branding_job(status)` |
| 5 | 🟡 P2 | Idempotency replay sync 2 queries on regenerate path | `RegenerateQuotaService.java:93-98` | Local Caffeine 10min window cache cho idempotency hash |

---

## DB Migration Audit

| Migration | Status | Indexes |
|-----------|:------:|---------|
| V28 beta_access_request | ✅ PASS | idx_email, idx_status, idx_token UNIQUE + partial; (email,status) combined missing nhưng idempotent submit OK |
| V29 regenerate_usage | ✅ PASS | uk_user_window UNIQUE + idx_idempotency + idx_window_end |
| V30 lifecycle_events | ✅ PASS | idx_instance_ts (instance_id, occurred_at DESC) |
| **MISSING** | ⚠️ | `BrandingJob.organization_name` (N+1 finding #1) + `BrandingJob.status` (poll loop #4) |

---

## Cache Opportunities

1. Quota reads → `@Cacheable` (P1 finding #2)
2. Slug availability → Local LRU 100-entry Caffeine 5min TTL (P0 finding #1)
3. BrandingJob.status lookups → DB index (P1 finding #4)
4. Token validation → ✅ đã có UNIQUE index trên invite_token (V28)

---

## Limitations

- KHÔNG runtime metrics (JMX/APM disabled per GAP-244)
- KHÔNG profiling P95/P99 latency hoặc GC impact
- KHÔNG load test — N+1 impact at 1K+ jobs extrapolated only
- Dev stack chưa fully boot — RabbitMQ queue-depth không observable
- FE bundle 149kB baseline acknowledged, chưa run `pnpm build --analyze`

---

## Gap Recommendations (state-check)

| Gap | Sev | New / Existing |
|-----|:---:|----------------|
| N+1 slug findAll() + index | 🔴 P0 | **NEW GAP** — không trùng existing |
| Quota cache + SSE backpressure + status index + idempotency cache | 🟠 P1 cluster | **NEW GAP** cluster |

---

## 1-line summary

Wave 34 ship 58/100 F (delta -5 vs baseline 63) — **N+1 slug findAll() là P0 BLOCKING production** (FE keystroke → full table scan); quota cache + SSE backpressure + status index = P1 cluster; ~5h dev fix N+1 + indexes + cache.
