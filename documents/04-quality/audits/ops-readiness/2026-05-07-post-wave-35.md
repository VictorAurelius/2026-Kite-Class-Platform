# Ops Readiness Audit — Post Wave 35

**Date:** 2026-05-07
**Auditor:** Explore subagent (ops-readiness-audit skill)
**Scope:** Wave 35 (GAP-387 Micrometer beta funnel counters + alert rules) — delta vs 2026-05-07 baseline 50/100
**Skill:** `.claude/skills/quality/ops-readiness-audit/SKILL.md`
**Output folder note:** lưu tại `audits/ops-readiness/` (skill SKILL.md hiện ghi `audits/ops/`; 3 file gần nhất sống trong `audits/ops/`); thư mục này khởi tạo mới theo task spec.

---

## Score: 53/100 — F (delta +3 vs 2026-05-07 baseline 50/100)

| # | Category (20 pts) | Pre-W35 | Post-W35 | Δ | Notes |
|---|-------------------|:-------:|:--------:|:--:|-------|
| 1 | Monitoring & Observability | 12 | 13 | +1 | 4 custom counters domain-level (`beta_signup_requests_total`, `beta_signup_approvals_total`, `beta_signup_rejections_total`, `beta_honeypot_rejections_total`) tagged by persona — đầu tiên có business-funnel metrics ngoài auto-instrumented HTTP. Vẫn không có Grafana dashboard deployed, không có distributed tracing. |
| 2 | Logging Standards | 8 | 8 | 0 | Wave 35 không touch logging; Logstash JSON encoder vẫn baseline; MDC `tenantId`/`betaRequestId` chưa wire (GAP-114 deferred Wave 7). |
| 3 | Backup & Recovery | 6 | 6 | 0 | Không có script `backup-production.sh` (Wave 33 P1 NEW recommendation chưa ship). RTO/RPO vẫn undefined. `verify-restore.sh` exists nhưng pre-deploy snapshot automation thiếu. |
| 4 | Alerting | 10 | 12 | +2 | 2 alert rules mới (`BetaSignupRateAnomaly`, `BetaHoneypotSpike`) wired đúng Helm + Docker mirror, có runbook_url + 2 runbook files, mirror parity OK. AlertManager production receivers vẫn PARTIAL (GAP-144). |
| 5 | Deployment Pipeline | 14 | 14 | 0 | Không thay đổi; smoke test `scripts/smoke-test.sh` 0 reference đến `/api/v1/beta` — không cover beta-signup flow (Wave 33 P1 carry). |

**Total: 53/100 (F)** — beta funnel metrics + alerts wired correctly, nhưng platform vẫn ops-incomplete cho production: backup automation thiếu, smoke test không cover beta flow, AlertManager receivers chưa live.

---

## GAP-387 verification — closed CÓ ĐIỀU KIỆN

✅ **Counters emitted đúng spec:**
- `BetaAccessService.java:65-68` — 4 metric name constants
- `BetaAccessService.java:89-114` — 4 counter builders với persona tag + cardinality bounded (Bean Validation enforced vocabulary)
- `BetaAccessService.java:150` — `signupCounter().increment()` trong `submitRequest()`
- `BetaAccessService.java:230` — `approvalCounter().increment()` trong `approveRequest()`
- `BetaAccessService.java:252` — `rejectionCounter().increment()` trong `rejectRequest()`
- `BetaAccessService.java:181-184` — `recordHoneypotRejection()` method exposed

✅ **Alert rules wired đúng 2 nơi:**
- `infrastructure/helm/kitehub/templates/prometheusrule.yaml:379-397` — BetaSignupRateAnomaly + BetaHoneypotSpike
- `kitehub/docker/prometheus/alert-rules.yml:258,268` — mirror parity (5 matches greppd)
- Cả 2 rule có `runbook_url` annotation

✅ **Runbooks tồn tại:**
- `documents/05-guides/operations/runbooks/beta-signup-rate-anomaly.md` (51 lines, 4 likely-cause taxonomy + diagnostic steps + remediation table)
- `documents/05-guides/operations/runbooks/beta-honeypot-spike.md` (2.1K)

⚠️ **HONEYPOT COUNTER NEVER INCREMENTS — DEAD ALERT:**

`recordHoneypotRejection()` ở `BetaAccessService.java:181` được declare PUBLIC nhưng **không có callsite trong codebase**:

```
$ grep -rn "recordHoneypotRejection" kitehub/kitehub-subscription/src/main/java
BetaAccessService.java:181:    public void recordHoneypotRejection() {
```

Service docstring (line 178-179) thừa nhận: *"Wire-up from controller / @ExceptionHandler is tracked as part of the same wave (Bucket A scope)"* — nhưng grep `BetaAccessController.java` + `@ExceptionHandler.*honeypot` + `MethodArgumentNotValidException` trong package `beta/` đều cho 0 hit. Bean Validation `@Size(max=0)` trên honeypot field reject submission → 400 trả về client → counter KHÔNG được increment → `beta_honeypot_rejections_total` perpetually 0 → `BetaHoneypotSpike` alert là **dead rule** (rate luôn 0/h, không thể trigger).

GAP-387 status `🟢 DONE 2026-05-07` vi phạm `gap-done-discipline.md` §2 criterion 2 (*"counter emitted khi event xảy ra"*) cho 1/4 counters. Đáng lẽ là 🟡 PARTIAL với follow-up gap mở cho honeypot wire-up.

---

## Top findings

### 🔴 P0 NEW — Honeypot counter wire-up missing → dead alert
- **File:** `kitehub/kitehub-subscription/src/main/java/com/kitehub/subscription/beta/service/BetaAccessService.java:181`
- **Issue:** `recordHoneypotRejection()` method exists nhưng 0 callsite. Bean Validation `@Size(max=0)` từ `BetaRequestDto.java:38` reject ở MVC layer trước khi vào service → counter không bao giờ increment. Alert `BetaHoneypotSpike` (`prometheusrule.yaml:389`) là dead rule.
- **Fix:** Add `@RestControllerAdvice` (hoặc method-level `@ExceptionHandler` trong `BetaAccessController`) bắt `MethodArgumentNotValidException` cho path `/api/v1/beta/request`, inspect FieldError on `honeypot` → call `betaAccessService.recordHoneypotRejection()`.

### 🔴 P0 CARRY — AlertManager production receivers PARTIAL (GAP-144)
- **File:** `infrastructure/helm/kitehub/values.yaml:267-293`
- **Issue:** AlertManager subchart `enabled: true` nhưng `monitoring.alertmanager.receivers.production.enabled` vẫn flag-gated stub. Wave 35 thêm 2 alert rules mới — chúng fire vào /dev/null cho đến khi receivers wire (Slack/PagerDuty/email).
- **Tracked:** GAP-144 PARTIAL (chart-level OK, live deploy pending).

### 🔴 P0 CARRY — Backup automation thiếu (Wave 33 P1 NEW chưa ship)
- **File:** `scripts/` (no `backup-production.sh`)
- **Issue:** Pre-deploy DB snapshot không automated — checklist trong `documents/05-guides/operations/runbooks/` exists nhưng không có script trigger `aws rds create-db-snapshot` / `pg_dump`. RTO/RPO undefined. `BackupJobFailure` alert (`prometheusrule.yaml:260`) METRIC PENDING (`kite_backup_last_success_timestamp_seconds` không có producer).
- **Fix:** Create `scripts/backup-production.sh` + cron + pushgateway gauge emitter.

### 🟠 P1 NEW — Smoke test 0 coverage cho beta-signup flow
- **File:** `scripts/smoke-test.sh` (0 hits cho `beta-signup`/`beta_invite`/`/api/v1/beta`)
- **Issue:** Wave 33 ship beta invite mechanism (GAP-372), Wave 35 ship metrics observability — nhưng smoke test chỉ check `/actuator/health`. Beta endpoint regression sau deploy không bị catch.
- **Fix:** Extend smoke test: POST `/api/v1/beta/request` → assert 202 + `beta_signup_requests_total` counter increment trong `/actuator/prometheus`.

### 🟠 P1 CARRY — Logging structured JSON without MDC fields
- **File:** Helm `logback-spring.xml` references (logstash-encoder configured)
- **Issue:** `logs-format-standard.md` v1.0.0 spec exists; implementation deferred Wave 7 (GAP-114). `BetaAccessService.log.info(...)` lines 133/152/183/231/253 emit text-interpolated tenantId/email — không có MDC `tenantId`/`betaRequestId`/`persona` structured fields. Multi-tenant issue isolation vẫn impossible.

### 🟠 P1 CARRY — Frontend error tracking thiếu
- **File:** `kitehub/kitehub-frontend/src/app/{error,global-error}.tsx`
- **Issue:** Next.js error boundaries exist nhưng không wire Sentry/equivalent — error tracking trong production chỉ qua console. Frontend regression silent.

### 🟡 P2 — kube-prometheus-stack disabled by default
- **File:** `infrastructure/helm/kitehub/values.yaml:130`
- **Issue:** `monitoring.enabled: false` mặc định; production cần explicit opt-in. Documentation ở `values.yaml:212` hướng dẫn flag override nhưng nếu operator quên flag thì PrometheusRule + ServiceMonitor không deploy → toàn bộ alert rules (kể cả GAP-387) không active. Wave 33 baseline finding carry.

### 🟡 P2 — `recordHoneypotRejection` log level mismatch
- **File:** `BetaAccessService.java:183`
- **Issue:** Log level `WARN` cho mỗi single bot probe → nếu wire xong mà có bot scan, log noise có thể spam. `INFO` hợp lý hơn; aggregate-level WARN nên ở alert side (đã có `BetaHoneypotSpike`).

### 🟢 P3 — `MultiTenantDataLeak` METRIC PENDING
- **File:** `prometheusrule.yaml:236`
- **Issue:** Critical alert nhưng `tenant_isolation_violations_total` counter chưa có producer trong `BaseEntity` filter. Carry baseline.

---

## Twelve-Factor compliance (delta vs Wave 33)

| Factor | Wave 33 | Wave 35 | Note |
|--------|:-------:|:-------:|------|
| III. Config in env | 🟡 | 🟡 | Không thay đổi |
| IV. Backing services | 🟢 | 🟢 | RDS/Redis/SES via env |
| IX. Disposability | 🟡 | 🟡 | Readiness 30s, no startup probe |
| XI. Logs as event streams | 🟡 | 🟡 | Logstash configured nhưng MDC chưa wire |

---

## Per-pillar verdict

| Pillar | Verdict |
|---|---|
| Monitoring | Cải thiện rõ — beta funnel có domain metrics, nhưng dashboards 0, tracing 0 |
| Alerting | Wave 35 wired đúng, nhưng 1/2 alert là dead rule (honeypot) + AlertManager receivers PARTIAL |
| Backup/DR | Đứng yên — vẫn là điểm đen lớn nhất (no automation, no RTO/RPO) |
| Deploy Pipeline | Đứng yên — smoke test thiếu cover beta flow |
| Logging | Đứng yên — chờ Wave 7 GAP-114 |
| SLO | Tier-A/B/C/D rules sẵn từ GAP-135; chưa có per-beta-endpoint SLO label |

---

## Tổng kết

Wave 35 đạt mục tiêu hẹp (beta funnel metrics + alert rules + runbooks) → +3 điểm. Nhưng:

1. **GAP-387 không thực sự DONE** — 1/4 counter (honeypot) không có wire-up callsite, alert dead.
2. **Wave 33 P0/P1 NEW gaps chưa ship** — backup automation, beta-signup smoke test, AlertManager receivers vẫn open.
3. **Phase 1 BETA blocker** — observability stack (kube-prometheus-stack disabled-by-default + AlertManager receivers PARTIAL) vẫn block production deploy.

Lift 50→53 (+3) đúng prediction "55+" thấp hơn 2 điểm vì honeypot wire-up missing không boost được Monitoring full +2 và Alerting +2 (lẽ ra chỉ +2 thực sự fire). Để đạt 55+ cần: wire honeypot exception handler (+1 Monitoring) + ship `backup-production.sh` (+1 Backup) + extend smoke-test (+1 Deploy).
