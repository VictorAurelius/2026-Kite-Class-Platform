---
title: Wave 1 G2 walk recipe — 5 P0 local (GAP-1066/1115/1139/1213/1308)
date: 2026-06-19
phase: phase-1-beta
campaign: phase-1-closeout Wave 1
status: G1-done — G2-browser-pending (human)
seed: bash kitehub/scripts/seed-walk-tenant.sh (tenant g2walk)
---

# Wave 1 G2 recipe — 5 P0 local-walk

**Setup (1 lần):** stack up (`bash kitehub/scripts/up.sh`) + `bash kitehub/scripts/seed-walk-tenant.sh`.
Access: KC `http://g2walk.127.0.0.1.nip.io:3000` · KH `http://localhost:3001` · gateway `:9000`.
Creds: owner `g2walk@kite.local / G2walk@2026` · teacher `huong.nguyen@g2walk.vn / Teacher@2026` · parent `phuhuynh@g2walk.vn / Parent@2026`.

## GAP-1308 — gateway strip X-User-Roles (role-spoof) — ✅ G1 DONE (curl attack-sim sanctioned)
**G1 evidence:** config `RemoveRequestHeader=X-User-Roles/X-User-Email` active (application.yml L983/987); runtime forged `X-User-Roles: OWNER` no-token → **401** on `/api/v1/admin/instances` + `/api/platform/admin/beta-requests` (stripped/denied). Attack-sim = curl is the correct method (not FE affordance) per g1-browser-walk §note. **→ flip DONE.**

## GAP-1066 — attendance status normalize — ✅ G1 DB-verified, G2 browser pending
**G1 evidence:** `attendance` all UPPERCASE (PRESENT/ABSENT/LATE/EXCUSED); V87+V88 `success=true`; `chk_attendance_status` constraint present; kiteclass-core healthy (no crash-loop).
**G2 (human):** teacher login KC → class "Lớp Anh A1 GV" → màn điểm danh → ghi PRESENT/ABSENT/LATE/EXCUSED/MAKEUP → save OK + reload giữ giá trị. Terminal: 0 error, status UPPERCASE in DB.

## GAP-1139 — KC OWNER tenant-admin (403→200) — ✅ G1 API-verified, G2 browser pending
**G1 evidence:** owner token (`/api/auth/login`, role=OWNER) → `GET /api/v1/reports/attendance` + `/reports/revenue` via gateway = **200** (not 403). OWNER→ROLE_OWNER mapping works.
**G2 (human):** owner login KC → /reports + roster điểm danh → trang load 200 có data (KHÔNG 403). Sad-path: teacher (không phải owner) → vẫn 403 ở admin-only.

## GAP-1115 — LMS paywall bypass — ⏸️ G1 BLOCKED (cần seed)
**Blocker:** `lessons`=0 trong tenant (7 paid courses nhưng 0 lesson). Cần seed ≥1 paid lesson + ≥1 trial lesson + 1 student KHÔNG enroll khóa paid.
**G1 prereq:** seed LMS content + student-non-enrolled → `GET /lms/courses/{id}/structure` (student JWT) → assert paid lesson `content==null && videoUrl==null`; trial lesson full body.
**G2 (human):** student chưa mua → mở khóa paid → chỉ thấy metadata (title/order), KHÔNG xem được content/video; bài trial xem được.

## GAP-1213 — wizard deploy propagate theme→KC — ⏸️ G1 needs flow
**State:** `landing_pages.branding_version`=0 (chưa deploy nào propagate). Cần chạy full wizard → deploy.
**G1/G2 (human):** owner KH → branding wizard → chọn theme → Deploy → DoneStep COMPLETED → check `landing_pages.branding_version` tăng + landing tenant render theme mới. Pre-walk check: RabbitMQ `branding.events` exchange + KC consumer + `RabbitConfig` idClassMapping (wire-format FQN→record). Risk: ClassNotFoundException drop message (Bug #4 class).

## Outcome
- **DONE this wave:** GAP-1308 (G1 curl-sanctioned complete).
- **G1-verified, G2-quick (human ~2min each):** GAP-1066, GAP-1139.
- **Needs test-data/flow setup before walk:** GAP-1115 (LMS seed), GAP-1213 (wizard run).
