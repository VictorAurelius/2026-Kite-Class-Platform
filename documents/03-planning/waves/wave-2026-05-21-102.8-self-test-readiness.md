---
title: Wave 102.8 — Self-Test Readiness Foundation
status: complete
created: 2026-05-21
updated: 2026-05-21
waves: [102.8]
gaps: [GAP-692, GAP-694, GAP-695]
audience: dev
---

# Wave 102.8 — Self-Test Readiness Foundation

**Goal:** Unblock local self-test (Tier 0 + Tier 1) + ship `env-reference.yaml` tooling foundation, để Wave 102.9 có thể execute AWS rebuild SOP doc (GAP-693).
**Trigger:** Outside-in synthesis 2026-05-21 (3 parallel agents) → 4 gaps filed (GAP-692/693/694/695) → user direction "đưa ra wave hoàn thiện" same-day session 2026-05-21.
**Estimated wall-clock:** ~3.5h critical path (A 1.5h + C/D parallel 2h); Bucket B 1 dev-day parallel suốt wave (longest bucket).

---

## 1. Brainstorm (5-10 min)

**Q1 (alignment):**
- **Inside-out source (canonical ROADMAP §🚀):** 4 gaps user-named explicit (GAP-692/693/694/695).
- **Inside-out queue file** (`documents/03-planning/inside-out-queue.md`): NOT cross-referenced trong wave này — scope locked theo user direction "đọc gap 692 đến 695". Items khác trong queue defer Wave 102.9+.
- **Outside-in synthesis ALREADY happened 2026-05-21** (per `outside-in-coverage-trigger.md` §4 exception "audit gần đây ≤30 ngày"): 3 parallel agents (Failure-mode F-1 + External benchmark E-1 + Persona simulation P-1) produced the 4 gaps. Outside-in coverage = satisfied.
- **Personas served:** Solo dev (anh Kiệt) Phase 1 BETA — chặn self-test loop → chặn beta invite velocity.

**Q2 (trade-offs considered, rejected):**
- **Mega-wave bundle GAP-693 Phase 1 doc cùng wave này:** REJECTED per user direction. GAP-693 hard-blocked theo §Status (GAP-612 + GAP-694 DONE + GAP-692 Phase 1 DONE) — ship doc trước khi 3 prereqs đáp ứng = doc shape có thể sai. Defer Wave 102.9 sau khi A/C/D DONE.
- **Skip Bucket C (codify rule) "vì rule có thể ship sau":** REJECTED. Per `incident-to-rule-pipeline.md` Stage 3 — rule + enforcement same PR. Codify rule trong cùng wave Bucket A unlock = enforcement parity.
- **GAP-695 Tier 1+2+3 full execution cùng wave:** REJECTED. Tier 2+3 ~9-10h scope quá rộng. Wave 102.8 chốt Tier 0+1; Tier 2 defer Wave 102.9 (sau khi Bucket C rule landed + Tier 1 endpoint+auth green).

**Q3 (risks + recovery):**
- **Bucket A Docker launch fail trên Windows host (UAC prompt / Hyper-V init):** Recovery = agent retries powershell.exe Start-Process; nếu fail 3 lần → escalate user manual click. Pivot signal per `release-fix-retry-budget.md` §4 row "Tooling visibility gap".
- **Bucket B TF `var.domain_name` mismatch fix breaks terraform plan:** Recovery = revert TF var change; ship env-reference.yaml + render script independently. TF fix re-defer sub-gap.
- **Bucket D admin login flow surfaces NEW bug (GAP-518 deeper than 97%):** Recovery = scope-revise per `pre-handoff-self-test-completeness.md` §2.4 — close GAP-518 PARTIAL ở mức stack-up verified; file new gap cho deeper issue.
- **Concurrent session lock conflict (3 prior sessions today):** Mitigation = check `ls .claude/session-locks/` + co-ordinator merge sequential A→B→C→D; agents `run_in_background: true` worktree isolation per `agent-background-spawn-default.md`.

---

## 2. Task Breakdown

| Bucket | Gap(s) | Owner | Effort | Disjoint? |
|--------|--------|-------|--------|-----------|
| A | GAP-694 Phase 0B | bg-agent | ~1.5h | ✅ `kitehub/scripts/` + `.env` only |
| B | GAP-692 Phase 1 | bg-agent | ~1 dev-day | ✅ `documents/02-architecture/env-reference.yaml` + `scripts/` + `infrastructure/terraform-aws/variables.tf` + new rule file |
| C | GAP-694 Phase 0C | bg-agent | ~30min | ✅ `.claude/rules/local-self-test-before-aws-deploy.md` only |
| D | GAP-695 Tier 1 | bg-agent | ~2h | ✅ Live walk + `kitehub-frontend/src/.../admin/sidebar` + close GAP-518/519 |

Disjoint check: 4 buckets touch disjoint paths. Bucket A ships `check-docker.sh` (preflight); Bucket C codify rule khi A done; Bucket D consume stack từ A. Bucket B independent — env tooling foundation.

---

## 3. Scope (compact schema)

**Stake tier:** MEDIUM → model: Opus medium (default Sonnet 4.6 OK; coordinator Opus 4.7).
**Cross-layer? NO** — không có FE+BE đồng thời consume new endpoint. Bucket D consume existing endpoint (admin login already shipped GAP-518 PARTIAL 97%). Skip Bucket 0 Foundation per `contract-first-for-cross-layer.md` §2.

| # | Bucket | Gap(s) | Priority | Files (glob) | Spawn order |
|:-:|--------|--------|:--------:|--------------|:-----------:|
| 1 | **A** | GAP-694 Phase 0B | 🔴 P0 | `kitehub/scripts/check-docker.sh` (new), `kitehub/scripts/up.sh` (edit), `kitehub/scripts/setup.sh` (edit), `kitehub/.env` (populate 9 keys — gitignored) | First — unblocks C+D |
| 2 | **B** | GAP-692 Phase 1 | 🟠 P1 META | `documents/02-architecture/env-reference.yaml` (new), `scripts/render-env-vars.sh` (new), `scripts/check-unresolved-env-vars.sh` (new), `.claude/rules/markdown-variable-reference.md` (new v1.0.0), `infrastructure/terraform-aws/variables.tf` (edit — `var.domain_name` `kiteclass.com` → `kitehub.me` + add `var.aws_account_id` + `var.secrets_prefix`), `.github/workflows/script-quality.yml` (add `env-vars-render` job), `.claude/rules/_examples/env-reference-self-test.md` (new) | PARALLEL with A |
| 3 | **C** | GAP-694 Phase 0C | 🟠 P1 META | `.claude/rules/local-self-test-before-aws-deploy.md` (new v1.0.0), `.claude/rules/_examples/` self-test fixture | Sequential after A |
| 4 | **D** | GAP-695 Tier 1 + GAP-518 + GAP-519 + GAP-481 | 🔴 P0 | Live walk (no file change tier-1 verify) + `kitehub/kitehub-frontend/src/app/admin/_components/Sidebar.tsx` (add `/admin/beta-requests` link), `kitehub/kitehub-frontend/src/.../*-spec.ts` (verify), gap status updates | Sequential after A |

### Bucket A — Docker preflight + .env populate (GAP-694 Phase 0B)

- Files: `kitehub/scripts/check-docker.sh` (new ~50 LOC bash), `kitehub/scripts/up.sh` line 1 (integrate preflight call), `kitehub/scripts/setup.sh` (integrate), `kitehub/.env` (append 9 keys per audit `2026-05-21-local-self-test-investigation.md` Finding 2 dev-safe defaults; gitignored)
- Pre-action: agent launches Docker Desktop via `powershell.exe -NoProfile -Command "Start-Process 'C:\\Program Files\\Docker\\Docker\\Docker Desktop.exe'"` per `agent-action-bias.md` §1 Part B
- Tests: `bash kitehub/scripts/check-docker.sh` exit 0 (post Docker launch); `bash kitehub/scripts/up.sh --profile infra-only` → 4 services healthy
- Acceptance:
  - [ ] `docker version` reachable inside WSL kite-dev
  - [ ] `kitehub/scripts/check-docker.sh` shipped + 3-fixture self-test PASS (docker-running / docker-stopped / auto-launch)
  - [ ] `up.sh` + `setup.sh` integrate preflight (FAIL fast khi daemon unreachable)
  - [ ] `.env` populate 9 keys + `kitehub/scripts/up.sh --profile infra-only` → Postgres/Redis/RabbitMQ/MinIO healthy (`docker ps` 4+ services UP)
  - [ ] GAP-694 status flip PARTIAL 15% → PARTIAL 75% (Phase 0B DONE, Phase 0C deferred Bucket C)

### Bucket B — env-reference tooling + TF mismatch fix (GAP-692 Phase 1)

- Files: per §3 row 2 globs
- Tests: `bash scripts/render-env-vars.sh production documents/02-architecture/env-reference.yaml documents/05-guides/deploy/release-1-deploy-plan.md` → render output byte-identical với hand-substituted control file; `bash scripts/check-unresolved-env-vars.sh` exit 0 on `_examples/env-reference-self-test.md` clean fixture
- Acceptance:
  - [ ] `env-reference.yaml` ship với ≥10 rows (aws_account_id + aws_region + domain_root + support_email + db_name + db_user + secret_prefix + ecr_uri + iam_deploy_role + cloudtrail_name)
  - [ ] `render-env-vars.sh` + `check-unresolved-env-vars.sh` ship + self-test PASS
  - [ ] `.claude/rules/markdown-variable-reference.md` v1.0.0 codified + cross-link `production-env-config-registry.md` v1.1.1
  - [ ] TF mismatch fix: `var.domain_name` default `kiteclass.com` → `kitehub.me` (HOẶC remove default force explicit) + add `var.aws_account_id` + `var.secrets_prefix`; `cd infrastructure/terraform-aws && terraform validate` PASS
  - [ ] `.github/workflows/script-quality.yml` job `env-vars-render` validates 1 sample rendered doc identical
  - [ ] GAP-692 status flip OPEN 0% → PARTIAL 33% (Phase 1 DONE; Phase 2+3 defer Wave 103+)

### Bucket C — Codify local-self-test-before-aws-deploy rule (GAP-694 Phase 0C)

- Files: `.claude/rules/local-self-test-before-aws-deploy.md` v1.0.0 (new) + `.claude/rules/_examples/local-self-test-self-test-fixture.md`
- Tests: §6 worked self-test apply rule retroactively to Wave 71b admin-login incident — verify rule would have caught role-guard mismatch locally before deploy
- Acceptance:
  - [ ] Rule v1.0.0 codified với mandate `bash scripts/local/smoke-e2e.sh` PASS before AWS deploy workflow trigger
  - [ ] Override trailer `LOCAL_SMOKE_SKIP: <reason + follow-up gap>` mechanism per `incident-to-rule-pipeline.md` Stage 5
  - [ ] `paths:` frontmatter scope `.claude/rules/**` + AWS deploy workflow files
  - [ ] Rule cross-link với `pre-handoff-self-test-completeness.md` §2.4 + `agent-action-bias.md` §1 Part A
  - [ ] GAP-694 status flip PARTIAL 75% → DONE 100% (Phase 0A+0B+0C all shipped)
  - [ ] `rules-index.csv` row added per `meta-csv-index-pattern.md`
  - [ ] `output-review-mandate.md` §3 matrix row "Local self-test before AWS deploy" added

### Bucket D — GAP-695 Tier 1 execution (endpoint + auth)

- Files: Live walk + minimal FE edit `Sidebar.tsx` cho GAP-519 nav link
- Tests: Live admin login walk per `pre-handoff-self-test-completeness.md` §2.4 (a)→(g) checklist:
  - (a) Credential `admin@kitehub.me / <password>` available trong handoff
  - (b) `POST /api/auth/login` HTTP 200 + JWT
  - (c) Login UI → redirect `/admin`
  - (d) Admin role-guard accepts (no 403/blank)
  - (e) Sidebar có link `/admin/beta-requests`
  - (f) `/admin/beta-requests` page renders với data
  - (g) Approve action POST `/api/v1/admin/beta-requests/{id}/approve` HTTP 200 + UI update
- Acceptance:
  - [ ] Stack up (Bucket A done) + admin login flow PASS §2.4 (a)→(g)
  - [ ] GAP-518 status flip PARTIAL 97% → DONE 100% (live verify evidence)
  - [ ] GAP-519 sidebar nav link shipped + verified
  - [ ] GAP-481 gateway routing `/api/v1/admin/*` 404 → 200 verified
  - [ ] GAP-695 status flip PARTIAL 25% → PARTIAL 50% (Tier 0+1 DONE; Tier 2+3 defer Wave 102.9+)

---

## 4. State-Check Evidence (BẮT BUỘC per `audit-to-gap-pipeline.md` §2.6)

| Symbol | Type | Verification command | Evidence | Verdict |
|--------|------|----------------------|----------|---------|
| `kitehub/scripts/check-docker.sh` | New script Bucket A | `ls kitehub/scripts/check-docker.sh` | not found 2026-05-21 | 🆕 to-be-created (Bucket A) |
| `kitehub/scripts/up.sh` | Edit Bucket A | `head -20 kitehub/scripts/up.sh` | exists (Wave 37 GAP-407 profile system) | ✅ exists |
| `kitehub/scripts/setup.sh` | Edit Bucket A | `ls kitehub/scripts/setup.sh` | exists (GAP-417/426 secrets hardening) | ✅ exists |
| `kitehub/.env` | Populate Bucket A | `ls kitehub/.env` | does not exist (gitignored; created by setup.sh) | 🆕 to-be-created (Bucket A — gitignored) |
| `kitehub/.env.example` | Reference Bucket A | `ls kitehub/.env.example` | not found at expected path | ⚠️ audit Finding 2 references this file; verify actual canonical (`kitehub/.env.production.template` exists at repo root) before agent spawn |
| `documents/02-architecture/env-reference.yaml` | New Bucket B | `ls documents/02-architecture/env-reference.yaml` | not found | 🆕 to-be-created (Bucket B) |
| `scripts/render-env-vars.sh` | New Bucket B | `ls scripts/render-env-vars.sh` | not found | 🆕 to-be-created (Bucket B) |
| `scripts/check-unresolved-env-vars.sh` | New Bucket B | `ls scripts/check-unresolved-env-vars.sh` | not found | 🆕 to-be-created (Bucket B) |
| `infrastructure/terraform-aws/variables.tf` | Edit Bucket B | `grep -n "var.domain_name\|var.aws_account_id\|var.secrets_prefix" infrastructure/terraform-aws/variables.tf` | per GAP-692 §Current State row: `var.domain_name = "kiteclass.com"` STALE exists; `var.aws_account_id` + `var.secrets_prefix` missing | ✅ exists (edit) + 🆕 add 2 vars |
| `.claude/rules/markdown-variable-reference.md` | New Bucket B v1.0.0 | `ls .claude/rules/markdown-variable-reference.md` | not found | 🆕 to-be-created (Bucket B) |
| `.claude/rules/local-self-test-before-aws-deploy.md` | New Bucket C v1.0.0 | `ls .claude/rules/local-self-test-before-aws-deploy.md` | not found | 🆕 to-be-created (Bucket C) |
| `.claude/rules/production-env-config-registry.md` | Cross-link Bucket B | `ls .claude/rules/production-env-config-registry.md` | exists v1.1.1 | ✅ exists |
| `.claude/rules/pre-handoff-self-test-completeness.md` §2.4 | Cross-link Bucket C+D | `grep -n "§2.4 admin-flow" .claude/rules/pre-handoff-self-test-completeness.md` | exists v1.1.1 §2.4 7-item checklist | ✅ exists |
| `kitehub/kitehub-frontend/src/app/admin/_components/Sidebar.tsx` | Edit Bucket D | `find kitehub/kitehub-frontend/src/app/admin -name "Sidebar*"` | verify-at-spawn-time (agent confirm) | ⚠️ verify-at-spawn (Bucket D first action) |
| `GAP-518` | Live verify Bucket D | `bash scripts/query-gaps.sh GAP-518` OR `grep "^GAP-518" documents/04-quality/gaps/gap-status.csv` | per start-session CSV: PARTIAL 97% | ✅ exists |
| `GAP-519` | Live verify Bucket D | `grep "^GAP-519" documents/04-quality/gaps/gap-status.csv` | per GAP-695 §Tier 1: PARTIAL P1 admin sidebar | ✅ exists |
| `GAP-481` | Live verify Bucket D | `grep "^GAP-481" documents/04-quality/gaps/gap-status.csv` | per GAP-695 §Tier 1: OPEN gateway routing 404 | ✅ exists |
| `documents/04-quality/audits/local-stack/2026-05-21-local-self-test-investigation.md` | Phase 0A audit Bucket A reference | `ls documents/04-quality/audits/local-stack/2026-05-21*.md` | exists (Phase 0A shipped) | ✅ exists |

Forward-looking exceptions: Bucket A creates `.env` (gitignored, NOT committed) + `check-docker.sh`; Bucket B creates 3 scripts + YAML + rule; Bucket C creates 1 rule. All 🆕 verdicts paired với explicit Bucket ownership.

Outstanding verify-at-spawn: `kitehub/.env.example` canonical path (audit references this; actual file may be `.env.production.template` — Bucket A first action confirm); `Sidebar.tsx` exact path (Bucket D first action grep).

---

## 5. Verification Gates (per bucket)

| Bucket | Local verify command | CI gate |
|--------|---------------------|---------|
| A | `bash kitehub/scripts/check-docker.sh && bash kitehub/scripts/up.sh --profile infra-only && docker ps` | None (local-only scope; script-quality CI lints check-docker.sh shellcheck) |
| B | `bash scripts/render-env-vars.sh production documents/02-architecture/env-reference.yaml _examples/env-reference-self-test.md` + `cd infrastructure/terraform-aws && terraform validate` | `script-quality.yml` job `env-vars-render` (new) + existing `shellcheck` |
| C | `bash scripts/check-rule-frontmatter.sh` + manual review §2.4 checklist applied retroactively | `script-quality.yml` job `rule-frontmatter` + `rule-staleness` |
| D | Live admin login walk per `pre-handoff-self-test-completeness.md` §2.4 (a)→(g) + `pnpm -F @kitehub/frontend lint && pnpm -F @kitehub/frontend test:unit` | `frontend-ci` per `.github/workflows/kitehub-frontend-ci.yml` |

---

## 6. Agent Spawn Pattern

Per `feedback_parallel_agent_strategy.md` + `agent-background-spawn-default.md`:
- 4 buckets spawned `run_in_background: true` + `isolation: worktree`
- RELATIVE paths trong agent prompts per `feedback_worktree_absolute_path_contamination.md`
- Spawn order:
  1. **Sequential first:** Bucket A (Docker launch + .env + preflight)
  2. **Parallel với A from start:** Bucket B (env-reference tooling — independent)
  3. **Sequential after A:** Bucket C (codify rule) + Bucket D (Tier 1 verify) — both consume A's stack-up
- Coordinator merges sequential A → B → C → D after all background completions

**Special handling for Bucket A:**
- Agent runs `powershell.exe -NoProfile -Command "Start-Process 'C:\\Program Files\\Docker\\Docker\\Docker Desktop.exe'"` via Bash tool (per agent-action-bias.md §1 Part B command-over-UI)
- Wait 30-60s for Docker Desktop boot + verify `docker version` reachable
- Nếu 3 retry fail → escalate user manual + file follow-up gap per `release-fix-retry-budget.md` §4 row "Tooling visibility gap"

**Concurrent mutation safety per `concurrent-production-mutation-ops.md`:**
- Bucket A `.env` write + Bucket B TF `variables.tf` edit = local-only mutations, NOT production
- TF `variables.tf` edit = file-state change only (NO `terraform apply`); production AWS untouched
- Production state-check N/A for this wave (no AWS mutation scope)

---

## 7. Closure Protocol

Per `gap-done-discipline.md` + `feedback_post_merge_doc_sync.md` + `feedback_wave_history_append_required.md` + `post-wave-cleanup.md` + `wave-closure-scope-completeness.md`:

- Each bucket PR updates affected GAP file Log + Status + CSV row sync per `post-merge-sync-completeness.md` 4 targets
- ROADMAP §🚀 Next Action updated trong closure PR (cite GAP-695 Tier 2/3 defer Wave 102.9)
- Wave plan frontmatter `status: draft → complete` flip trong closure PR
- `wave-history.jsonl` append entry với `outcome` summary (Rule 15 enforcement)
- **Scope-completeness reconciliation table per `wave-closure-scope-completeness.md` §3** — closure PR body MUST enumerate mỗi §3 Scope item categorized ✅ DONE / 🟡 PARTIAL / ❌ NOT-IMPLEMENTED + follow-up gap link
- Sub-gaps filed cho mọi defer (GAP-695 Tier 2 Wave 102.9, GAP-693 Wave 102.9 prereq path); PARTIAL exit-ramp per `gap-done-discipline.md` §3
- `bash scripts/prune-merged-worktrees.sh --yes` after all 4 bucket PRs merged (before drafting closure PR)
- **`## Release Plan Progress` section trong closure PR body** per `feedback_wave_closure_release_progress_report.md`: current Phase 1 BETA + milestone progress + wave contribution (self-test loop unlocked) + estimated remaining wall-clock cho v0.9.0-beta trigger

---

## 8. Log

- **2026-05-21** (draft): Plan created. Triggered by user direction same-day session "đọc gap 692 đến 695 để đưa ra wave hoàn thiện" sau outside-in synthesis 2026-05-21 (3 parallel agents F-1 + E-1 + P-1) produced 4 gaps. 4-bucket structure (A 1.5h sequential + B 1 dev-day parallel + C 30min sequential after A + D 2h sequential after A). Critical path ~3.5h wall-clock. GAP-693 (rebuild SOP doc) deferred Wave 102.9 per user AskUserQuestion confirmation. Outside-in audit ALREADY satisfied ≤30 ngày exception per `outside-in-coverage-trigger.md` §4. Inside-out queue file NOT consulted (scope locked user explicit subset); inside-out from canonical CSV state verified 2026-05-21.
