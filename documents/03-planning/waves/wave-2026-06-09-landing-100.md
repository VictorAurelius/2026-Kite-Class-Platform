---
title: Wave landing-100 — KiteClass per-tenant landing "đẹp 100% mọi tenant" + seed demo-trio thesis
status: draft
created: 2026-06-09
updated: 2026-06-09
tag_primary: landing-100
gaps: [GAP-810, GAP-828, GAP-138, GAP-826, GAP-958, GAP-595, GAP-596, GAP-274, GAP-805]
references:
  - documents/02-architecture/tenant-domain-landing-architecture.md
  - documents/08-thesis/chapter-4-deployment-results.md
  - documents/02-architecture/design-system/ui_kits/marketing-site/README.md
---

# Wave landing-100 — Landing per-tenant "đẹp 100%" + seed demo-trio

## 1. Brainstorm (inside-out + outside-in)

**Mục tiêu user (2026-06-09):** landing KiteClass "UI đẹp 100%" + "seed data như thesis đã trình bày".

**Insight hội tụ (3 outside-in agent — persona + benchmark + failure-mode):** rào cản số 1 cho "đẹp 100% **mọi** tenant" = landing fallback **DEMO data bịa** khi tenant chưa seed (GV giả, giá giả, "500+ HV 95%" giả, review giả). "Đẹp nhưng giả + sai sự thật". → seed demo-trio thật + empty-state cho tenant khác = bắt buộc.

**Điểm landing hiện tại:** ~64/100 (tenant đã cấu hình) · ~55 (chưa) → ITERATE.

**Reconcile target (chốt 2026-06-08):** apex `kitehub.me` (kitehub-frontend) = Claude Design marketing kit; `{slug}` subdomain (kiteclass-frontend) = per-tenant template. Wave này chỉ làm **per-tenant landing** (kiteclass-frontend `(public)`); marketing-kit port apex = scope riêng.

**Seed demo-trio (thesis §4.1-4.2):**
| Tenant | Gói | Môn | Branding | Hình |
|---|---|---|---|---|
| Cô Đỗ Lan Khánh | (walkthrough §4.1) | — | — | 4.1 |
| Cô Nguyễn Thị Hà | Miễn phí | Toán tiểu học | **xanh dương**, template (no AI) | 4.3 |
| Thầy Nguyễn Đình Nhì | Trả phí | Hóa THCS | **xanh lá**, AI Branding | 4.4 |

## 2. Scope — 7 bucket (completion verdict per audit-to-gap-pipeline.md §2.6.1)

| Bucket | Scope | Files chính | Gap | Verdict | Walk |
|---|---|---|---|---|---|
| **A. Anti-fabrication** | Bỏ mọi DEFAULT data bịa trong section → empty-state thật / ẩn section (bug-class 1 chuẩn `emptyState`). Gate "go-live" khi thiếu cấu hình bắt buộc | `components/sections/{Teachers,Pricing,Testimonials,Stats,Certificates,Faq}*` + `page.tsx` slot logic | GAP-958 (cache leak) | 🆕 Greenfield (empty-state mới) | FE ✅ |
| **B. Brand + contact** | Nav/footer dùng tên trung tâm (field riêng, KHÔNG `heroTitle`); ẩn `1900 xxxx`/`support@kiteclass.com` placeholder khi chưa cấu hình | `(public)/layout.tsx` + `page.tsx` contact defaults + plumb `centerName` | — | 🔨 Delta (layout exists) | FE ✅ |
| **C. Hero rework** | **AI scene/chân dung làm ảnh NỀN + slogan/CTA render HTML overlay (React), KHÔNG bake text vào PNG** (vs `compose-teacher-banner.mjs` bake → mobile không reflow) + `next/image` optimized. Giữ chất lượng AI scene + đẹp mobile + chữ Việt crisp | `components/sections/HeroSection.tsx` | GAP-810 (PARTIAL, hero assets) | 🔨 Delta (Hero exists) | FE ✅ |
| **D. Theme safety** | Contrast guard WCAG AA (clamp lightness text/CTA) + **SSR-inline CSS var (hết FOUC + fix theme-không-áp-khi-reload — BrandingThemeApplier post-hydration useEffect + react-query refetchOnWindowFocus)** + `--theme-cta` derive từ palette + **fix `themeReceiver.ts` allowlist thiếu `localhost:3000` (console spam preview)** | `components/theme/{ThemeSync,BrandingThemeApplier}.tsx` + `lib/postMessage/themeReceiver.ts` + `globals.css` + theme util mới | — (discovery G2 2026-06-09) | 🆕 Greenfield (guard + SSR-inline) | FE ✅ |
| **E. Per-tenant SEO/OG** | `generateMetadata` title/JsonLd theo tên+logo tenant + OG image (`opengraph-image` next/og) + canonical per-subdomain | `(public)/page.tsx` generateMetadata + `opengraph-image.tsx` + sitemap | — | 🔨 Delta (metadata generic exists) | FE ✅ |
| **F. Port Claude Design kit** | Problem→Solution + How-it-works 3-step + benefit-card + trust strip + scroll-reveal + floating CTA + Zalo/click-to-call (kit ~88/100 → per-tenant) | `components/sections/` (NEW: ProblemSolution, HowItWorks, TrustStrip, FloatingCTA) + `TemplateRenderer.tsx` | GAP-828/595/596/274 | 🆕 Greenfield (section mới) | FE ✅ |
| **G. Seed demo-trio** | Seed 3 tenant Khánh + Hà (free/xanh dương) + Nhì (paid/xanh lá) **TRỰC TIẾP vào entity `Branding`** (màu + banner AI-scene sẵn `public/demo-banners/` + text theo thesis) — KHÔNG qua AI wizard (FULL_AI→landing persist vỡ: GAP-1021 phase-2). Branding + lớp/GV/HS/học phí/điểm/điểm danh khớp Hình 4.1/4.3/4.4 | seed (`kiteclass-core` `BrandingDataSeeder.java` + SQL) | GAP-805 (Sky→trio) | 🔨 Delta (Sky demo exists) | DB ✅ |

## 3. State-Check Evidence

| Symbol | Verify | Verdict |
|---|---|---|
| `(public)/page.tsx` TemplateRenderer + slot logic | đã đọc — render section từ `slots` + `nonEmptyArray` fallback DEMO | ✅ exists (Bucket A re-wire) |
| `(public)/layout.tsx:27` `name: landing.heroTitle` | đã đọc — brand = heroTitle | ✅ exists (Bucket B fix) |
| `HeroSection.tsx` baked-text PNG | benchmark agent cite dòng 82-128 | ✅ exists (Bucket C rework) |
| `ThemeSync.tsx` useEffect post-hydration | failure-mode agent cite FOUC + no contrast guard | ✅ exists (Bucket D fix) |
| section DEFAULT data (Teachers/Pricing/...) | failure-mode agent cite fabricated fallback | ✅ exists (Bucket A) |
| marketing-site kit (Problem→Solution, AppMock, 3-step) | benchmark agent cite chưa port → page.tsx | 🆕 to-port (Bucket F) |
| GAP-813 endpoint `by-subdomain` | verified live (`PublicTenantController.java`) | ✅ (không block wave) |
| Sky demo tenant (GAP-805 DONE) | CSV DONE | ✅ Delta → demo-trio (Bucket G) |

## 4. Execution — parallel agents (Opus, worktree-isolated, ≤5 concurrent)

**Đợt 1 (4 agent song song — file disjoint):**
- Agent C: HeroSection.tsx (own file)
- Agent D: ThemeSync.tsx + globals.css + theme util (own files)
- Agent F: NEW section files + TemplateRenderer (mostly new files)
- Agent G: seed backend (kiteclass-core, disjoint khỏi FE)

**Đợt 2 (1 agent — page.tsx/layout.tsx/sections cùng vùng, tránh conflict):**
- Agent A+B+E: section empty-state (Teachers/Pricing/Testimonials/Stats/Certificates/Faq) + layout brand/contact + page.tsx SEO/metadata — batch cùng agent vì chung page.tsx/sections.

Mỗi agent: per `agent-model-opus-default` Opus + `fe-build-local-verify` (`pnpm --filter kiteclass-frontend build` PASS) + `cross-flow-bug-class-sweep` (A empty-state là bug-class → sweep mọi section).

## 5. Verification Gates

| Gate | Cách |
|---|---|
| G1 (Claude) | `pnpm --filter kiteclass-frontend build` + test PASS mỗi bucket; browser-walk `:3000` 3 tenant demo-trio (per `g1-browser-walk-before-flip`) — render đẹp + KHÔNG demo bịa |
| G2 (human) | Recipe riêng sau G1 PASS (per `g2-handoff-md-mandate`): walk landing 3 tenant + empty-state tenant rỗng + theme contrast + mobile hero |
| G3 (parity) | landing đạt rubric ≥90/100 (re-score sau wave); OG share Zalo OK |

## 6. Closure
- Flip GAP-810/828/958/595/596 DONE sau G1+G2; GAP-274 (apex marketing kit) giữ riêng phase-2.
- Re-score landing rubric (mục tiêu ≥90/100 từ ~64).
- Sync gap-status.csv + ROADMAP + wave-history (per `post-merge-sync-completeness`).

## 7. Sequencing constraint
⚠️ Wave này rebuild kiteclass-frontend nhiều lần → **CHỜ user xong KC-1 G2 walk** (đang chạy trên `:3000`) rồi mới spawn agents + rebuild. Cũng land **sau** khi PR #2274 (3 CI fix + GAP-1077 port) merge.
