---
title: Wave 102.9 — Self-Test Completion (Tier 2 + Tier 3 email + Gateway JWT fix)
status: complete
created: 2026-05-21
updated: 2026-05-21
waves: [102.9]
gaps: [GAP-538, GAP-637, GAP-620, GAP-531, GAP-516, GAP-543, GAP-659, GAP-657, GAP-699, GAP-695]
audience: dev
---

# Wave 102.9 — Self-Test Completion (Tier 2 + Tier 3 email + Gateway JWT fix)

**Goal:** Đóng Tier 2 business flow + Tier 3 email-critical + GAP-699 gateway JWT_SECRET passthrough fix để GAP-695 PARTIAL 50% → ~85% + Wave 102.8.1 self-test §2.4 item (g) approve action unblocked — dev có thể self-test full owner persona walk (signup → email welcome → tenant init → onboarding wizard 5-step → invite staff + admin RBAC + approve action qua gateway port 9000) trước khi mời beta cohort.
**Trigger:** User direction 2026-05-21 post Wave 102.8 closure "draft wave luôn" cho Tier 2 + Tier 3 email + Wave 102.8.1 closure "include GAP-699 fix into Wave 102.9" cho compose JWT_SECRET fix.
**Estimated wall-clock:** ~3h critical path (Bucket E sequential first ~5min → A+B+C+D song song; Bucket B longest ~2-3h).

---

## 1. Brainstorm (5-10 min)

**Q1 (alignment):**
- **Inside-out source:** GAP-695 catalog Tier 2 + Tier 3 (đã file 2026-05-21 từ outside-in synthesis 3-agent F-1 + E-1 + P-1).
- **Outside-in skip rationale per `outside-in-coverage-trigger.md` §4 exception:** outside-in audit ALREADY happened 2026-05-21 (cùng ngày, ≤30 days) cho cluster gap này; user explicit "draft wave luôn" sau khi đã xem Tier 2+3 breakdown trong báo cáo Wave 102.8 = explicit acknowledgment of scope. Per §5 banned shortcut "User explicit từ chối outside-in" → ghi nhận skip với lý do.
- **Inside-out queue check:** GAP-695 catalog đã aggregate inside-out items per Wave 102.8 brainstorm. NOT re-consulted `inside-out-queue.md` (scope locked từ catalog).
- **Personas served:** Phase 1 BETA — Owner (chị Hằng) signup → onboarding wizard → tenant init → invite staff (email tone). Admin (anh Kiệt) trigger RBAC checks + approve flow. 2FA admin login hardening.
- **Tier 3 scope subset:** chỉ email-critical gaps (GAP-543/659/657). VN data seed (GAP-658) + student endpoints (GAP-269b) + FE polish (GAP-138/139) defer Wave 102.10 vì:
  - GAP-658 phụ thuộc onboarding seed shape (Wave 102.9 Bucket B → 102.10 enrich)
  - GAP-269b kc-student persona Phase 1.5+ priority, không block beta
  - GAP-138/139 FE polish không block Tier 2 business verify

**Q2 (trade-offs):**
- **Single mega-wave Tier 2+3 full (12 gaps):** REJECTED. 12 gap × ~1h trung bình = ~12h scope vượt wave-pack ≥3 buckets parallel pattern. Chia 2 wave gọn hơn.
- **Wave 102.9 = Tier 2 only (4 gap), Tier 3 hoàn toàn Wave 102.10:** REJECTED partial. Email gaps (GAP-543/659/657) là dependency cho beta cohort invite (chị Hằng nhận email professional, không phải Lorem Ipsum). Bao gồm email trong 102.9 unlock beta-cohort-ready signal.
- **Gộp GAP-693 (rebuild SOP) + GAP-698 (audit + OIDC) vào 102.9:** REJECTED. GAP-693 hard-blocked GAP-612 AWS chưa giải quyết. GAP-698 Phase 1 ops-readiness audit (deadline ≤2026-05-24 per `post-wave-audit-mandate.md` §2.4 domain-milestone `release-deploy-artifacts`) sẽ tách Wave 102.10 cùng GAP-693 để gộp domain audit scope.
- **Spawn 6 bucket song song:** REJECTED. Per `feedback_parallel_agent_strategy.md` max 4-5 agents parallel. Cluster 4 bucket disjoint path safe hơn.

**Q3 (risks + recovery):**
- **Bucket A admin v1 controllers @PreAuthorize annotation conflict với existing tests:** Recovery = chạy `./mvnw verify` trên agent worktree trước commit; nếu fail → reduce scope sang 1 controller, file follow-up.
- **Bucket B tenant init walk discovers cross-service bug (kitehub-subscription ↔ kiteclass-core handoff):** Recovery = file P0 follow-up gap, ship Bucket B PARTIAL với evidence + scope subset.
- **Bucket D email template rebuild requires kitehub-email image rebuild:** Recovery = `bash kitehub/scripts/rebuild.sh kitehub-email` similar Wave 102.8.1 pattern; nếu fail → defer 1 email template sang Wave 102.10.
- **Concurrent gap-status.csv writes giữa 4 bucket:** Mitigation = mỗi bucket update CSV rows non-overlapping (A→GAP-637/620, B→GAP-531/538, C→GAP-516, D→GAP-543/659/657 + GAP-695); nếu conflict → coordinator rebase resolution per Wave 102.8 lesson.
- **FE image stale lessen Bucket B onboarding wizard verify:** Mitigation = Bucket B agent runs `bash kitehub/scripts/rebuild.sh kitehub-frontend` first nếu Wave 102.8.1 chưa land OR pull rebuilt image từ ECR.

---

## 2. Task Breakdown

| Bucket | Gap(s) | Owner | Effort | Disjoint? | Order |
|--------|--------|-------|--------|-----------|:-----:|
| **E** | GAP-699 | bg-agent | ~5-10min | ✅ `kitehub/docker-compose.kitehub.yml` only | **FIRST** (sequential, unblock gateway) |
| A | GAP-637 + GAP-620 | bg-agent | ~1h | ✅ kitehub-admin/branding BE @PreAuthorize + ITs | parallel after E |
| B | GAP-531 + GAP-538 | bg-agent | ~2-3h | ✅ kitehub-subscription tenant + kitehub-frontend onboarding + seed | parallel after E |
| C | GAP-516 | bg-agent | ~30min | ✅ kitehub-subscription 2FA TOTP flow | parallel after E |
| D | GAP-543 + GAP-659 + GAP-657 | bg-agent | ~1.5h | ✅ kitehub-email templates + headers | parallel after E |

Disjoint check:
- Bucket E = compose file only (single 2-line edit `kitehub/docker-compose.kitehub.yml` `kite-gateway:` block) — disjoint mọi bucket khác
- Bucket B + C đều touch `kitehub-subscription` nhưng:
  - Bucket B = tenant + onboarding endpoint scope (`TenantInitController`, `OnboardingController`)
  - Bucket C = 2FA challenge + login flow scope (`AuthController`, `TwoFactorService`)
- File-disjoint OK. Nếu agent spawn time-overlap chạm cùng package → coordinator rebase pattern Wave 102.8.

**Spawn dependency:** Bucket E ship FIRST (~5-10min) → gateway healthy lại → A/B/C/D có thể verify approve action + RBAC 403 + 2FA challenge + email send qua gateway port 9000 trong AC. Without E first, A/B/C có thể PARTIAL do gateway crash.

---

## 3. Scope (compact schema)

**Stake tier:** MEDIUM-HIGH → model: Opus medium (Bucket B Opus full do complexity tenant init e2e walk).
**Cross-layer? PARTIAL** — Bucket B touch FE (onboarding wizard) + BE (tenant init endpoint). Existing `api-contract.md` cho cả 2 domain đã có (Wave 78+98). Per `contract-first-for-cross-layer.md` §2: Bucket 0 Foundation NOT required vì contracts đã ship; reviewer verify api-contract reference trong Bucket B AC.

| # | Bucket | Gap(s) | Priority | Files (glob) | Spawn order |
|:-:|--------|--------|:--------:|--------------|:-----------:|
| 0 | **E** (Gateway JWT fix) | GAP-699 | 🟠 P1 | `kitehub/docker-compose.kitehub.yml` only (2-line edit `kite-gateway:` block: add `JWT_SECRET` + `ENCRYPTION_MASTER_KEY` env passthrough) | **FIRST sequential** (unblocks A/B/C/D gateway-dependent verify) |
| 1 | **A** | GAP-637 + GAP-620 | 🔴 P0 | `kitehub/kitehub-admin/src/main/java/.../controller/**`, `kitehub/kitehub-branding/src/main/java/.../controller/**`, `kitehub/kitehub-subscription/src/main/java/.../controller/admin/**`, matching `src/test/java/.../it/AdminAuthorizationIT.java` | Parallel batch 2 after E |
| 2 | **B** | GAP-531 + GAP-538 | 🔴 P0 | `kitehub/kitehub-subscription/src/main/java/.../tenant/**` + `kitehub/kitehub-frontend/src/app/(onboarding)/**` + `kitehub/scripts/seed-day1-data.sh` | Parallel batch 2 after E |
| 3 | **C** | GAP-516 | 🟠 P1 | `kitehub/kitehub-subscription/src/main/java/.../auth/twofactor/**` + `kitehub/kitehub-frontend/src/app/(auth)/2fa/**` | Parallel batch 2 after E |
| 4 | **D** | GAP-543 + GAP-659 + GAP-657 | 🟠 P1 | `kitehub/kitehub-email/src/main/resources/templates/**` + `kitehub/kitehub-email/src/main/java/.../config/EmailHeadersConfig.java` | Parallel batch 2 after E |

### Bucket E — Gateway JWT_SECRET passthrough fix (GAP-699)

- Files: `kitehub/docker-compose.kitehub.yml` `kite-gateway:` service block (2-line edit)
- Add `JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required - run ./scripts/setup.sh}` (matching subscription line 364 + admin line 516 pattern)
- Add `ENCRYPTION_MASTER_KEY: ${ENCRYPTION_MASTER_KEY:?ENCRYPTION_MASTER_KEY is required - run ./scripts/setup.sh}` (consistency với security secrets group)
- Restart gateway: `docker restart kite-gateway && bash kitehub/scripts/wait-for-healthy.sh`
- Verify gateway port 9000:
  - `curl -sI http://localhost:9000/actuator/health` → HTTP 200
  - `curl -X POST http://localhost:9000/api/auth/login -d '{"email":"admin@kitehub.com","password":"Admin@KiteHub123"}'` → HTTP 200 + JWT (E2E gateway → subscription)
  - `JWT=$(curl ...)` rồi `curl -X POST -H "Authorization: Bearer $JWT" http://localhost:9000/api/v1/admin/beta-requests/{id}/approve` → HTTP 200 (closes Wave 102.8.1 item (g) PARTIAL)
- Acceptance:
  - [ ] `kite-gateway:` block trong compose có `JWT_SECRET` + `ENCRYPTION_MASTER_KEY` passthrough
  - [ ] Gateway container healthy sau restart (no `IllegalStateException`)
  - [ ] Curl login via port 9000 → HTTP 200 + JWT
  - [ ] Curl approve action via port 9000 với Bearer JWT → HTTP 200
  - [ ] Wave 102.8.1 §2.4 item (g) re-verify PASS với evidence trong PR body
  - [ ] GAP-699 OPEN → DONE 100% + `git mv` to `phase-1-beta/closed/`

### Bucket A — Admin RBAC @PreAuthorize backfill (GAP-637 + GAP-620)

- Files: per §3 row 1 globs
- Backfill `@PreAuthorize("hasRole('ADMIN')")` cho mọi controller `/api/v1/admin/*` endpoint chưa có (GAP-637 audit identify list specific)
- Add 403 integration tests verify non-admin user → 403 Forbidden
- GAP-620 live verify 3 controllers post-rebuild (kitehub-admin / kitehub-branding / kitehub-subscription admin scope) qua curl với JWT role≠ADMIN → expect 403
- Acceptance:
  - [ ] Mọi `/api/v1/admin/*` controller có `@PreAuthorize` annotation
  - [ ] 403 IT pass per controller (non-admin JWT → 403)
  - [ ] `./mvnw -pl kitehub-admin,kitehub-branding,kitehub-subscription verify -P strict-warnings` PASS
  - [ ] Curl evidence: non-admin JWT → 403; admin JWT → 200/400 (per existing route)
  - [ ] GAP-637 PARTIAL 60% → DONE 100% / GAP-620 OPEN → DONE 100%

### Bucket B — Tenant init e2e + Day-1 onboarding (GAP-531 + GAP-538)

- Files: per §3 row 2 globs
- GAP-531: walk `POST /api/v1/tenants` admin-approve → tenant created → subdomain resolution → JWT issued for tenant-scoped admin → first login → see empty dashboard
- GAP-538: Day-1 onboarding wizard 5-step VN checklist (chuẩn `user-manual-content-standard.md` §2 row 7 VN sample data) + IMPORT_DATA opt-in seed (existing Playwright E2E spec từ Wave 101 Bucket D)
- Verify FE rebuild prereq: Wave 102.8.1 đã rebuild image — nếu chưa land, agent rebuild trước
- Acceptance:
  - [ ] Curl `POST /api/v1/tenants` → 201 + tenant_id + subdomain
  - [ ] DB row verify: `tenants` table + `tenant_admins` seeded
  - [ ] Subdomain resolution: `curl http://{tenant}.localhost:8088/api/v1/auth/login` → 200
  - [ ] Onboarding wizard 5 steps render đúng VN narrative + sample data (`Trần Thị Hồng`, `Trung tâm Sky Education`, `Lớp 5A1`)
  - [ ] Playwright E2E `onboarding-wizard.spec.ts` PASS local
  - [ ] GAP-531 PARTIAL 50% → DONE 100% / GAP-538 PARTIAL 95% → DONE 100%

### Bucket C — 2FA Platform Admin TOTP (GAP-516)

- Files: per §3 row 3 globs
- Verify TOTP setup flow: admin first login → QR code render → scan TOTP app (Google Authenticator/Authy fixture) → enter 6-digit → 2FA enabled cho subsequent login
- Verify TOTP challenge flow: post-login enabled, next login `POST /api/v1/auth/login` → 202 (Accepted, 2FA required) + `challenge_id` → `POST /api/v1/auth/2fa/verify` + 6-digit → 200 + JWT
- Alternative scope: nếu TOTP infra chưa đủ → document disable trong dev profile (rule §3 OK cho local) + file follow-up GAP-XXX
- Acceptance:
  - [ ] TOTP setup endpoint reachable + QR code valid
  - [ ] Challenge flow verify (admin login → 2FA → JWT)
  - [ ] OR: document disable in dev `application-dev.yml` + follow-up gap filed
  - [ ] GAP-516 PARTIAL 80% → DONE 100% (verify path) HOẶC PARTIAL 90% (disable doc path)

### Bucket D — Email content + tone + hardening (GAP-543 + GAP-659 + GAP-657)

- Files: per §3 row 4 globs
- GAP-543: Audit 5 critical email types (welcome / invoice / staff-invite / password-reset / 2FA-challenge) content + tone Vietnamese per `vn-localization-audit-checklist.md` §2 Vietnamese label + §3 VN sample + §4 cultural awareness
- GAP-659: Staff-invite email persona-tone split — formal Owner (`Em chào chị/anh`) vs informal Teacher (`Chào em`) per `vn-localization-audit-checklist.md` Section 2 email tone matrix
- GAP-657: Email layer hardening — plain-text fallback + `List-Unsubscribe` header + `Reply-To` header + render verify (Mailhog UI hiển thị đúng cả HTML + plain-text variant)
- Acceptance:
  - [ ] 5 email templates render PASS qua Mailhog (http://localhost:8025) — Vietnamese narrative + VN sample data + persona tone correct
  - [ ] `List-Unsubscribe` + `Reply-To` headers verified qua Mailhog message inspect
  - [ ] Plain-text fallback rendered cho mọi template
  - [ ] GAP-543 + GAP-659 + GAP-657 đều PARTIAL 80% → DONE 100%

---

## 4. State-Check Evidence (BẮT BUỘC per `audit-to-gap-pipeline.md` §2.6)

| Symbol | Type | Verification command | Evidence | Verdict |
|--------|------|----------------------|----------|---------|
| `@PreAuthorize` cho `/api/v1/admin/*` controllers | Annotation | `grep -rn "@PreAuthorize\|@RequestMapping.*admin" kitehub/kitehub-admin/src/main/java kitehub/kitehub-branding/src/main/java kitehub/kitehub-subscription/src/main/java/.../admin` | Per GAP-637: missing trên 3 controllers; verify-at-spawn-time | ⚠️ verify-at-spawn (Bucket A first action) |
| `TenantInitController` | Java controller | `grep -rln "TenantInitController\|@PostMapping.*tenants" kitehub/kitehub-subscription/src/main/java` | Per Wave 30+ shipped | ✅ exists (verify path at spawn) |
| `OnboardingController` + onboarding wizard route | FE + BE | `find kitehub/kitehub-frontend/src/app -type d -name "*onboarding*" && grep -rln "OnboardingController" kitehub/kitehub-subscription/src/main/java` | Wave 78 + Wave 101 shipped | ✅ exists (verify path) |
| `seed-day1-data.sh` | Script | `ls kitehub/scripts/seed-day1-data.sh` | Per GAP-538 §Status PARTIAL 95% | ✅ exists |
| `onboarding-wizard.spec.ts` Playwright | E2E test | `find kitehub/kitehub-frontend/src -name "*onboarding*spec*"` | Per Wave 101 Bucket D | ✅ exists (verify path) |
| `TwoFactorService` + `2FA` route | BE + FE | `grep -rln "TwoFactor\|@PostMapping.*2fa\|/auth/2fa" kitehub/kitehub-subscription/src/main/java kitehub/kitehub-frontend/src/app` | Per GAP-516 PARTIAL 80% | ✅ exists (verify completeness) |
| 5 email templates (welcome/invoice/staff-invite/password-reset/2fa-challenge) | Email templates | `find kitehub/kitehub-email/src/main/resources/templates -name "*.html" -o -name "*.ftl"` | Per Wave 91 Bucket D admin email template + Wave 100 Bucket C signup | ✅ exists (verify content quality + tone) |
| `EmailHeadersConfig.java` `List-Unsubscribe` + `Reply-To` | BE config | `grep -rln "List-Unsubscribe\|Reply-To\|EmailHeadersConfig" kitehub/kitehub-email/src/main/java` | Per GAP-657 PARTIAL 80% | ⚠️ verify-at-spawn (Bucket D first action) |
| Mailhog UI port 8025 | Dev infrastructure | `docker ps | grep mailhog && curl -sI http://localhost:8025` | Per docker-compose.kitehub.yml mailhog service | ✅ exists (stack up dependency) |
| `vn-localization-audit-checklist.md` v1.0.0 | Rule | `ls .claude/rules/vn-localization-audit-checklist.md` | Wave 100 Bucket D shipped | ✅ exists |
| `pre-handoff-self-test-completeness.md` v1.1.1 §2.4 | Rule | `grep -n "§2.4 admin-flow" .claude/rules/pre-handoff-self-test-completeness.md` | Wave 71b shipped | ✅ exists |
| `local-self-test-before-aws-deploy.md` v1.0.0 | Rule | `ls .claude/rules/local-self-test-before-aws-deploy.md` | Wave 102.8 Bucket C shipped | ✅ exists |
| GAP-695 Tier 2 + Tier 3 catalog | Gap file | `grep -n "Tier 2\|Tier 3" documents/04-quality/gaps/phase-1-beta/GAP-695-self-test-readiness-comprehensive-plan.md` | Wave 102.8 ship | ✅ exists |
| `kitehub/docker-compose.kitehub.yml` `kite-gateway:` block | YAML compose service | `grep -A 30 "^  kite-gateway:" kitehub/docker-compose.kitehub.yml \| grep -E "JWT_SECRET\|ENCRYPTION_MASTER_KEY"` | Per GAP-699: MISSING — subscription line 364 + admin line 516 có pattern | ⚠️ verify-at-spawn (Bucket E first action) |
| GAP-699 gap file | Gap doc | `ls documents/04-quality/gaps/phase-1-beta/GAP-699-gateway-jwt-secret-compose-env-passthrough.md` | Wave 102.8.1 PR #1697 shipped | ✅ exists |
| Wave 102.8.1 audit artifact | Audit | `ls documents/04-quality/audits/local-stack/2026-05-21-wave-102-8-1-browser-walk-verify.md` | Wave 102.8.1 PR #1697 shipped | ✅ exists |

Forward-looking: Bucket A có thể tạo new `AdminAuthorizationIT.java` test files (🆕 to-be-created Bucket A). Bucket D có thể edit `EmailHeadersConfig.java` (verify-at-spawn — nếu chưa có, create new). Bucket B có thể edit `onboarding-wizard.spec.ts` (verify-at-spawn — nếu Wave 101 ship khác path).

---

## 5. Verification Gates (per bucket)

| Bucket | Local verify command | CI gate |
|--------|---------------------|---------|
| E | `docker restart kite-gateway && bash kitehub/scripts/wait-for-healthy.sh` + `curl -sI http://localhost:9000/actuator/health` (200) + login curl port 9000 (200 + JWT) | None (compose edit không trigger CI test workflow) |
| A | `cd kitehub && ./mvnw -pl kitehub-admin,kitehub-branding,kitehub-subscription verify -P strict-warnings` + curl 403 verify | `kitehub-ci` per `.github/workflows/kitehub-ci.yml` |
| B | `cd kitehub && ./mvnw -pl kitehub-subscription verify` + `pnpm -F @kitehub/frontend test:unit && pnpm -F @kitehub/frontend build` + Playwright `onboarding-wizard.spec.ts` PASS | `kitehub-ci` + `frontend-ci` |
| C | `cd kitehub && ./mvnw -pl kitehub-subscription verify -P strict-warnings` + curl 2FA challenge verify | `kitehub-ci` |
| D | `cd kitehub && ./mvnw -pl kitehub-email verify` + Mailhog UI inspect (http://localhost:8025) | `kitehub-ci` |

Tất cả bucket verify dùng `local-self-test-before-aws-deploy.md` v1.0.0 §3 evidence pattern trong PR body.

---

## 6. Agent Spawn Pattern

Per `feedback_parallel_agent_strategy.md` + `agent-background-spawn-default.md`:
- 5 buckets total; spawn 2 batches:
  - **Batch 1 (sequential):** Bucket E (`run_in_background: true` + `isolation: worktree`) — ship + merge FIRST (~5-10min)
  - **Batch 2 (parallel after E merged):** A + B + C + D spawn cùng lúc background worktree-isolated
- RELATIVE paths trong agent prompts per `feedback_worktree_absolute_path_contamination.md`
- Coordinator merges sequential E → A → B → C → D theo CI green order (whichever finishes first merges first, rebase others)

**Why E first:**
- Bucket A admin RBAC 403 IT verify cần gateway port 9000 (curl with JWT từ FE flow)
- Bucket B tenant init e2e walk + onboarding wizard verify cần gateway routing healthy
- Bucket C 2FA challenge flow verify cần gateway login endpoint
- Without E first, A/B/C có thể PARTIAL do gateway crash JWT_SECRET
- E là 2-line compose edit ~5-10min — cost của sequencing rất nhỏ

**Prereq:** Wave 102.8.1 đã merged (PR #1697 closed) — FE image `:latest` fresh + audit baseline + GAP-699 filed. ✅ (confirmed at plan revision time).

**Concurrent CSV writes safety per Wave 102.8 lesson:**
- E → GAP-699 row
- A → GAP-637 + GAP-620 rows
- B → GAP-531 + GAP-538 rows
- C → GAP-516 row
- D → GAP-543 + GAP-659 + GAP-657 rows
- All update GAP-695 catalog Log entry (potential conflict — coordinator rebase resolution at closure)

---

## 7. Closure Protocol

Per `gap-done-discipline.md` + `feedback_post_merge_doc_sync.md` + `feedback_wave_history_append_required.md` + `post-wave-cleanup.md` + `wave-closure-scope-completeness.md`:

- Each bucket PR updates affected GAP file Log + Status + CSV row sync per `post-merge-sync-completeness.md` 4 targets
- ROADMAP §🚀 Next Action updated trong closure PR (cite GAP-693 + GAP-698 + GAP-658 + GAP-269b + GAP-138 + GAP-139 defer Wave 102.10)
- Wave plan frontmatter `status: draft → complete` flip trong closure PR
- `wave-history.jsonl` append entry với `outcome` summary (Rule 15 enforcement)
- **Scope-completeness reconciliation table per `wave-closure-scope-completeness.md` §3** — closure PR body MUST enumerate mỗi §3 Scope item categorized ✅ DONE / 🟡 PARTIAL / ❌ NOT-IMPLEMENTED + follow-up gap link
- GAP-695 status flip PARTIAL 50% → PARTIAL ~85% (Tier 2 + Tier 3 email subset DONE; Tier 3 còn lại defer Wave 102.10)
- Sub-gaps filed cho mọi defer (Tier 3 còn lại + GAP-693 + GAP-698 prereq cho Wave 102.10); PARTIAL exit-ramp per `gap-done-discipline.md` §3
- `bash scripts/prune-merged-worktrees.sh --yes` after all 4 bucket PRs merged (before drafting closure PR)
- **`## Release Plan Progress` section trong closure PR body** per `feedback_wave_closure_release_progress_report.md`: current Phase 1 BETA + milestone progress + wave contribution (beta cohort signal: Tier 1 backend ✓ + Tier 1 browser ✓ Wave 102.8.1 + Tier 2 business flow ✓ Wave 102.9 + Tier 3 email ✓ Wave 102.9 = beta-cohort-ready khi Wave 102.9 closure)

---

## 9. Out-of-scope deferred Wave 102.10+

Per `gap-done-discipline.md` §3 PARTIAL exit ramp:

| Gap | Lý do defer | Wave target |
|---|---|---|
| GAP-658 (VN sample seed) | Phụ thuộc onboarding seed shape Wave 102.9 Bucket B; enrich sau khi shape ổn | Wave 102.10 |
| GAP-269b (kc-student endpoints) | Phase 1.5+ priority, không block beta Phase 1 | Wave 102.10 |
| GAP-138 (KiteClass landing hero) | FE polish 5min, batch với other FE | Wave 102.10 |
| GAP-139 (Parent Dashboard MVP) | ~2h scope độc lập, batch với GAP-138 | Wave 102.10 |
| GAP-693 (AWS rebuild SOP) | Hard-blocked GAP-612 AWS suspension + GAP-698 OIDC; gộp domain Wave 102.10 | Wave 102.10 |
| GAP-698 Phase 1 (ops-readiness audit) | Domain-milestone `release-deploy-artifacts` per `post-wave-audit-mandate.md` §2.4 — milestone = Wave 102.10 rebuild SOP closure | Wave 102.10 |
| GAP-698 Phase 2 (terraform-plan OIDC) | Tier 1 read-only investigation per `agent-aws-access.md`; gộp Wave 102.10 audit scope | Wave 102.10 |

**Promoted INTO Wave 102.9 (was Wave 102.10 candidate):**

| Gap | Lý do promote | Bucket |
|---|---|---|
| GAP-699 (gateway JWT_SECRET passthrough) | Phát hiện Wave 102.8.1 walk Finding #1; 2-line compose edit ~5-10min; unblocks A/B/C verify approve action + 403 IT + 2FA challenge qua gateway port 9000; sequential prereq cho A/B/C/D | Bucket E |

---

## 8. Log

- **2026-05-21** (draft v3 — wave-plan-completeness CI fix): Renumbered §8 Out-of-scope → §9 + §9 Log → §8 Log để khớp `scripts/check-wave-plan-completeness.sh` REQUIRED_SECTIONS literal grep `## 8. Log` (template canonical position per `_TEMPLATE.md` §8 = Log; custom Out-of-scope section đẩy xuống §9). Local re-run script PASS 21/21. Same-PR bundle staged auto-generated PR-1698.json (harness post-merge artifact from main) vào plan PR diff để cleanup main worktree.
- **2026-05-21** (draft v2 — GAP-699 promoted): Plan revised post Wave 102.8.1 PR #1697 merge. Promoted GAP-699 (gateway JWT_SECRET passthrough fix) từ Wave 102.10 defer candidate → Wave 102.9 Bucket E SEQUENTIAL FIRST (~5-10min compose 2-line edit). Triggered by user direction "include GAP-699 fix into Wave 102.9" post Wave 102.8.1 closure. Rationale: GAP-699 unblocks A/B/C verify approve action + 403 IT + 2FA challenge qua gateway port 9000 trong AC — without E first, A/B/C có thể PARTIAL do gateway crash JWT_SECRET. Bucket count 4 → 5. Spawn order revised: Batch 1 sequential E (~5-10min) → Batch 2 parallel A+B+C+D. Wave 102.8.1 prereq ✅ confirmed merged at revision time (FE image `:latest` fresh + audit baseline + GAP-699 filed).
- **2026-05-21** (draft v1): Plan created. Triggered by user direction post Wave 102.8 closure báo cáo "draft wave luôn" sau khi xem Tier 2 (4 gap ~3-4h) + Tier 3 (4 email gap ~1.5h) breakdown. Scope locked = Tier 2 + Tier 3 email subset (8 gap, 4 bucket parallel). Tier 3 còn lại (4 gap) + GAP-693 + GAP-698 defer Wave 102.10 (gộp rebuild SOP + audit domain milestone). Outside-in audit skipped per `outside-in-coverage-trigger.md` §4 exception (audit 2026-05-21 same-day ≤30 ngày; user explicit acknowledgment scope). Inside-out from GAP-695 catalog (aggregated). 4 bucket disjoint paths verified per §3 — Bucket B + C share `kitehub-subscription` package nhưng file-disjoint (tenant vs auth/2fa).
