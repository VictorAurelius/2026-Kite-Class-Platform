# Session handoff — 2026-06-07 (wave-p0-1 + wave-p0-2 + META §2.6.1 Bucket-Completion Check)

**Scope:** P0-over-G2 drive-down ([[project-p0-priority-over-g2]]). Executed wave-p0-1 (3 buckets) + wave-p0-2 (verify cluster) + shipped a META rule that surfaced repeatedly this session.

## Shipped (all merged to main)

| PR | Scope | Result |
|----|-------|--------|
| #2240 | **META** `audit-to-gap-pipeline.md` §2.6.1 Bucket-Completion Check (v1.5.0) + `_TEMPLATE.md` §4.1 | ✅ merged |
| #2241 | wave-p0-1: GAP-882 DONE + GAP-946/948 PARTIAL + GAP-1053 | ✅ merged |
| #2242 | wave-p0-2: GAP-942 PARTIAL + GAP-1054 | ✅ merged (docs) |

## Gap outcomes

- **GAP-882 🟢 DONE** — V92 `invoice_items.item_type` CHECK (V86 pre-fixed `invoices.status`); IT 1635 0-fail + G3 live DB walk (accept UPPERCASE / reject lowercase). Real new code.
- **GAP-946 🟡 PARTIAL 55%** — defensive `assertDatabaseProvisioned()` × 3 InstanceService sites. `provisionInfrastructure` real-impl = separate wave.
- **GAP-948 🟡 PARTIAL 70%** — wiring G3-verified live (tenant.deployed → consumer → outbox → EmailConsumer → kitehub-email received). MailHog residual = tenant-ready Thymeleaf template render error → **GAP-1053** (P1).
- **GAP-942 🟡 PARTIAL 40%** — V62 + PENDING semantics already shipped PR #2157; AC#1 verified live (started_at/expires_at nullable=YES). Runtime POST /subscriptions walk + regression IT residual.
- **GAP-1053 🔵 OPEN P1** — tenant-ready Thymeleaf template render error (EmailTemplateRenderer:110 throws).
- **GAP-1054 🔵 OPEN P2** — SubscriptionPendingNullableColumnsIT FK-parent-instance seed fix (agent-drafted, fails fk_subscription_instance).

**Phase 1 BETA P0: 29 → 28** (only GAP-882 flipped — net of session).

## Key META lesson (§2.6.1 fired 4×)

GAP-882/946/948/942 all looked "near-done" but were **already code-shipped**; residual = runtime-walk / template / config — NOT new code. The "drive-down P0 count" wave is largely **verify-walk** of code-complete gaps, blocked by runtime gates. §2.6.1 (new this session) catches this scope-vs-reality drift at wave-plan time prospectively.

## ⚠️ NEXT SESSION (fresh context — /clear recommended, was 88%)

P0 local-verifiable residuals need **runtime walks** (fresh context):
1. **GAP-975/976 webhook verify-walk** — set `SEPAY_API_KEY` in local kitehub-subscription env + restart → POST SePay webhook (Apikey + txnRef match) → verify reconcile + idempotency. Then flip DONE (28→26).
2. **GAP-942 runtime walk** — POST `/api/platform/subscriptions` BASIC on Owner FREE/TRIAL → verify 201 + PENDING persist + payment row + confirmPayment activation. Then flip DONE (→27/26).
3. **GAP-1053** — fix tenant-ready Thymeleaf template → re-publish tenant.deployed → MailHog render → closes GAP-948 AC#2.
4. **GAP-1054** — seed parent instance in IT → green.
5. **GAP-885 RLS** (wave-p0-2 deferred) — real migration work (kiteclass-core).
- AWS-gated cluster (793/502/608/533/567/566/572/117/756/952) needs `bash scripts/aws/start-stack.sh`.

## Stack state
Local prod-parity UP (kiteclass-core + kitehub-subscription rebuilt with V92 + InstanceService defensive this session). AWS STOPPED. `SEPAY_API_KEY` empty in local — needs setting for payment webhook walk.

## Language reminder
Respond tiếng Việt narrative + English identifiers (CLAUDE.md §Communication Language).
