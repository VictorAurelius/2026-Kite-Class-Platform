# Fixture: well-known defaults (no suspect pattern triggers)
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: ${CONTEXT_PATH:/api}
spring:
  application:
    name: ${APP_NAME:kitehub-service}
