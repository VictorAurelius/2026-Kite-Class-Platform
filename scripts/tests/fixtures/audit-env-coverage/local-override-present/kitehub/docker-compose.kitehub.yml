version: '3.8'
# Fixture: LOCAL dev compose WITH SIGNUP_BASE_URL override → CHECK B must NOT flag.
services:
  service:
    image: kitehub-service:latest
    environment:
      SERVER_PORT: 8080
      SIGNUP_BASE_URL: http://localhost:3000
